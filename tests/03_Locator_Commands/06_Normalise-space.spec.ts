import { test, expect } from "@playwright/test";

test('Start work with the normalise space function', async ({ page }) => {

    //XPath's normalize-space() function, you can use it to locate elements by text while ignoring extra spaces and leading/trailing whitespace.

});